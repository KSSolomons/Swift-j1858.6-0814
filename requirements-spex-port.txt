# Minimal dependencies for scripts/spex_port/
# Keep this separate from SAS/HEASOFT system tooling.
# The CLI-only SPEX workflow in scripts/spex_cli/ uses the standard library.
numpy>=1.24
pyspextools

