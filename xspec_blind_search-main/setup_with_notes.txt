### setup file for both gridsearch.tcl and the plotting python functions. 
# parameters/values/strings need to be added in the following order:
# 
# >>> The following are all strings used in defining how the output is saved:
# source
# instrument
# obsid
# binning
# modeltype
# 
# >>> The following are 1 or 0 depending on if you want to use c-statistics; freeze the continuum model; re-fit the continuum at the start; and save plots.
# cstat boolean
# freeze continuum boolean
# fit continuum boolean
# saveplots boolean
#
# >>> The minimum and maximum searched wavelengths in Angstrom, and the stepsize in the same units
# Lmin
# Lmax
# dL
# 
# >>> the width of the Gaussian
# velocitywidth in km/w
#
# >>> the model for the continuum, saved before with 'save model <>.xcm' in xspec
# continuum file, with @
#
# >>> the definition of the model including the line, in xspec speak.
# linemodel
#
# >>> the number of parameters of the full model:
# numpars (including gaussian line)
#
# >>> the actual .pha files for the spectra to analyse (more than one is possible, analysed together).
# spectra: one per line.
#
# NOTE: the line search contains a cutoff at 18.5 Angstrom (inclusive) when two RGS 
# spectra a loaded and the upper wavelength limit exceeds 18.5 Angstrom. This can 
# easily be changed by setting the value of RGS_maxA to a different value (such as $Lhigh)
# 
# NOTE: 0.05 stepsize is 0.01 for the actual science, but that is too slow for the simulations
###########################################################################################
1RXSJ180408
HEG
GainShift
bin1
1RXSJ180408_continuum_only_HEG_gainshift
1
1
0
0
4.13
12.4
0.01
1000.0
@1RXSJ180408_continuum_HEG_Gainshift.xcm
tbabs*(bbody+diskbb) + gauss
8
mydata_heg_pm_heg_abs1_grp20.pha